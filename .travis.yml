language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
# install:
#   - pip install -r requirements.txt
# # command to run tests
branches:
  only:
    - master
    - developer_2 
script:
  - pytest

after_success:
  - codecov